<Window x:Class="TrainStationServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="端口：" FontSize="20" Margin="20,0" HorizontalAlignment="Left" VerticalAlignment="Center"/> 
            <TextBox Grid.Column="0" Name="Port" Width="150" Height="30" Margin="20,0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Name="Start" Width="100" Height="30" Content="Start" Click="Start_Click_1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBox Name="Result" Margin="20"/>
        </Grid>
        
    </Grid>
</Window>
